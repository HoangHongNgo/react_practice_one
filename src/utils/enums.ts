export enum ButtonColor {
  Primary,
  Secondary,
  Tertiary,
}

export enum TextColor {
  Dark,
  Gray,
}

export enum HttpMethod {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}
